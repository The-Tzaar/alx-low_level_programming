#include "main.h"

/**
 * _islower - Checks if a character is lowercase.
 *
 * Return: 1 if the character is lowercase, 0 otherwise.
 * @c: i have to write something here.
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}

